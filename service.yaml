---
apiVersion: v1
kind: Service
metadata:
  namespace: demo-app-juiceshop
  name: demo-app-ajuiceshop-service
spec:
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app.kubernetes.io/name: demo-app-juiceshop
---